 version: '2'
 services:
   web:
     build: .
     command: python manage.py runserver 0.0.0.0:80
     volumes:
       - .:/code
       - /opt/backups/ketabkhar/db.sqlite3:/code/db.sqlite3
     ports:
       - "80:80"
